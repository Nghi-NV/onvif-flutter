name: onvif_ui_example
description: ONVIF Flutter UI Example

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  onvif_flutter:
    path: ../
  cupertino_icons: ^1.0.2
  fijkplayer: ^0.11.0
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
